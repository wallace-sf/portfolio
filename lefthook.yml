commit-msg:
  parallel: false
  commands:
    commitlint:
      run: npx commitlint --edit

pre-commit:
  parallel: false
  commands:
    format:
      run: CI=1 pnpm format:check
    lint:
      run: CI=1 pnpm lint:check
    types:
      run: CI=1 pnpm types
    test:
      run: CI=1 pnpm test
