---
title: Hello, blog
description: A placeholder post used to exercise SSG and rendering until real content is written.
---

# Hello, blog

This is a placeholder post. It exists so the listing and detail pages have something
real to render — a syntax-highlighted code block included for good measure:

```ts
export function hello(): string {
  return 'hello, blog';
}
```

Real posts land closer to launch.
