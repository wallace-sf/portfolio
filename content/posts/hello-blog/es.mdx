---
title: Hola, blog
description: Una publicación provisional usada para probar el SSG y el renderizado hasta que se escriba contenido real.
---

# Hola, blog

Esta es una publicación provisional. Existe para que las páginas de listado y detalle
tengan algo real que renderizar — un bloque de código con resaltado de sintaxis, de regalo:

```ts
export function hello(): string {
  return 'hello, blog';
}
```

Las publicaciones reales llegarán más cerca del lanzamiento.
